class Array:
	def __init__(self,a,length,capacity):
		self.a=[]
		self.length= length
		self.capacity = capacity

	def __check__(cap):
		arr=Array()
		if (cap <=0):
			raise Exception("Illegal capacity"+cap)
		cap= arr.capacity
		
	def get(arr):
		return arr.length()

	def set(index,ele):
		arr= Array()
		arr.a[index]= ele
	
